#ifndef DASH_H
#define DASH_H
#include <string>
#include <utility>
using namespace std;
class dash{
    public:
        void printDig(int i, std::pair<int, int> x);
        unsigned getNumDigits(unsigned i ); 
};


#endif